import React from "react";
import Auth from "../components/Auth/Auth";

const AuthScreen = ({ navigation, route }) => {
  return <Auth navigation={navigation} route={route} />;
};

export default AuthScreen;
