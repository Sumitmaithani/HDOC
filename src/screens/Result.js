import React from "react";
import Result from "../components/Result/Result";

const ResultScreen = ({ navigation, route }) => {
  return <Result navigation={navigation} route={route} />;
};

export default ResultScreen;
