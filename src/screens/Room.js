import React from "react";
import Room from "../components/Room/Room";

const RoomScreen = ({ navigation, route }) => {
  return <Room navigation={navigation} route={route} />;
};

export default RoomScreen;
