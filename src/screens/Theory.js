import React from "react";
import Theory from "../components/Theory/Theory";

const TheoryScreen = ({ navigation, route }) => {
  return <Theory navigation={navigation} route={route} />;
};

export default TheoryScreen;
