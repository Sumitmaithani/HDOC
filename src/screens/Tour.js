import React from "react";
import Tour from "../components/Tour/Tour";

const TourScreen = ({ navigation, route }) => {
  return <Tour navigation={navigation} route={route} />;
};

export default TourScreen;
